dynamic_resources:
  cds_config:
    path: /etc/xds/cds-active.yaml

  lds_config:
    path: /etc/xds/lds-active.yaml

admin:
  access_log_path: "/dev/null"
  address:
    socket_address:
      address: 0.0.0.0
      port_value: 8001

node:
  id: envoy
  cluster: envoy

static_resources:
  clusters:
  - name: google
    connect_timeout: 0.25s
    type: LOGICAL_DNS
    dns_lookup_family: V4_ONLY
    lb_policy: ROUND_ROBIN
    load_assignment:
      cluster_name: google
      endpoints:
      - lb_endpoints:
        - endpoint:
            address:
              socket_address:
                address: www.google.com
                port_value: 443
    tls_context:
      sni: www.google.com
